[
  {
    "task": "package-skeleton",
    "category": "packaging",
    "priority": 1,
    "done": true,
    "description": "R1 - Finish the installable src-layout package: console script, py.typed, and wheel-included lexicon data. The build backend, wheel target, requires-python, and description are ALREADY DONE, verified by a clean-env import.",
    "files_affected": [
      "pyproject.toml",
      "src/wordup/py.typed"
    ],
    "acceptance_criteria": [
      "ALREADY DONE: pyproject.toml declares [build-system] with hatchling and build-backend hatchling.build",
      "ALREADY DONE: [tool.hatch.build.targets.wheel] packages = [\"src/wordup\"], so the wheel installs wordup/ and not src/wordup/",
      "ALREADY DONE: requires-python is \">=3.11\"",
      "ALREADY DONE: description replaces the uv init placeholder",
      "[project.scripts] defines wordup = \"wordup.cli:main\"",
      "Package data configuration includes src/wordup/data/*.toml in the built wheel",
      "uv build succeeds and the resulting wheel contains wordup/data/",
      "py.typed marker is present and shipped",
      "A clean-env import still resolves: uv run --isolated --no-project --with ./dist/*.whl python -c \"import wordup\""
    ],
    "steps": [
      "Read pyproject.toml in full before changing anything; the build system is already configured and correct",
      "Do NOT set packages to [\"src\"]; that ships src/wordup/ in the wheel and breaks import wordup",
      "Add [project.scripts] entry wordup = \"wordup.cli:main\" only once src/wordup/cli.py exists, otherwise the entry point resolves to a missing module",
      "Add force-include or artifact configuration so src/wordup/data/*.toml lands in the wheel",
      "Leave src/wordup/__init__.py as is; it holds the legacy script and is replaced by the public-api task",
      "Create src/wordup/py.typed as an empty marker file",
      "Run uv build and verify the wheel contains wordup/ at top level plus wordup/data/",
      "Re-run the clean-env import check to confirm the wheel is still importable",
      "Commit: feat(packaging): complete package metadata and entry point"
    ]
  },
  {
    "task": "lexicon-data-migration",
    "category": "data",
    "priority": 2,
    "done": true,
    "description": "R2 - Migrate the 86 base words and 526 alternatives from the legacy script at src/wordup/__init__.py into src/wordup/data/lexicon.toml as a flat base-to-alternatives mapping",
    "files_affected": [
      "src/wordup/__init__.py",
      "src/wordup/data/lexicon.toml"
    ],
    "acceptance_criteria": [
      "lexicon.toml contains exactly 86 base words",
      "lexicon.toml contains exactly 526 alternatives in total",
      "Every word matches the source spelling character for character",
      "The four multi-word alternatives are preserved: deal with, apply for, make known, before long",
      "The hyphenated alternative clear-cut is preserved",
      "No score, weight, register, or tier field appears anywhere in the file",
      "A parity check confirms the parsed TOML equals the source dict exactly"
    ],
    "steps": [
      "Read the replacements dict in src/wordup/__init__.py in full",
      "Write a one-off conversion in the scratchpad that parses the dict and emits TOML",
      "Emit a flat mapping of base word to array of alternative strings",
      "Run a parity check asserting the parsed TOML equals the source dict exactly",
      "Verify counts: 86 bases, 526 alternatives, zero duplicates",
      "Commit: feat(data): migrate curated lexicon to shipped TOML data file"
    ]
  },
  {
    "task": "lexicon-loader-validation",
    "category": "core",
    "priority": 3,
    "done": true,
    "description": "R3 - Lexicon type that loads TOML via importlib.resources and enforces the five data invariants with typed exceptions",
    "files_affected": [
      "src/wordup/models.py",
      "src/wordup/lexicon.py",
      "src/wordup/errors.py",
      "tests/test_lexicon.py"
    ],
    "acceptance_criteria": [
      "Lexicon.default() loads shipped data via importlib.resources with no path argument",
      "Lexicon.load(path) accepts a Path so tests can inject fixtures",
      "Duplicate base word raises a typed exception naming the word",
      "An alternative appearing in more than one entry raises a typed exception naming the word",
      "A word that is both a base and an alternative raises a typed exception naming the word",
      "An entry listing its own base among its alternatives raises a typed exception",
      "An entry with an empty alternatives list raises a typed exception",
      "Malformed TOML raises a typed exception, never a bare tomllib error",
      "No user-facing flag exposes the path argument",
      "Tests cover every invariant and reach 100% coverage of lexicon.py"
    ],
    "steps": [
      "Create src/wordup/errors.py with a WordupError base and one subclass per failure mode",
      "Create src/wordup/models.py with the Lexicon dataclass and supporting types",
      "Implement Lexicon.load(path) using tomllib",
      "Implement Lexicon.default() using importlib.resources.files",
      "Implement validation for all five invariants, raising typed errors naming the offending word",
      "Write tests/test_lexicon.py covering each invariant with a fixture file",
      "Run ruff format, ruff check --fix, mypy src/, and pytest -x",
      "Commit: feat(lexicon): add validated TOML lexicon loader"
    ]
  },
  {
    "task": "inflection-rules",
    "category": "core",
    "priority": 4,
    "done": true,
    "description": "R4 - Suffix stemming and re-inflection expressed as data, conservative on failure, always withholding multi-word alternatives when a suffix was stripped",
    "files_affected": [
      "src/wordup/inflect.py",
      "tests/test_inflect.py"
    ],
    "acceptance_criteria": [
      "Suffix rules live in a module-level data structure, not in branching code",
      "Stemming handles -s, -es, -ed, -ing, and -ly",
      "Consonant doubling is handled, for example running to run",
      "Terminal-e restoration is handled, for example creating to create",
      "The stripped suffix is recorded and returned alongside the stem",
      "requires stems to require and re-inflects demand to demands",
      "creating stems to create and re-inflects generate to generating",
      "An alternative that cannot be confidently re-inflected is withheld, never emitted malformed",
      "Multi-word alternatives are always withheld when a suffix was stripped",
      "Multi-word alternatives are offered normally on an exact base-word match",
      "No output ever contains deal withs or apply fored",
      "Tests are parameterized over the suffix table and reach 100% coverage of inflect.py"
    ],
    "steps": [
      "Read src/wordup/models.py for the types established in lexicon-loader-validation",
      "Create src/wordup/inflect.py with the suffix rule table as data",
      "Implement stem(word) returning the candidate stem and the stripped suffix",
      "Implement reinflect(word, suffix) returning the inflected form or None when not confident",
      "Add the multi-word guard: any alternative containing a space returns None when a suffix was stripped",
      "Write tests/test_inflect.py parameterized over the rule table, including negative cases",
      "Run ruff format, ruff check --fix, mypy src/, and pytest -x",
      "Commit: feat(inflect): add conservative suffix stemming and re-inflection"
    ]
  },
  {
    "task": "protected-regions",
    "category": "core",
    "priority": 5,
    "done": false,
    "description": "R5 - Regex detection of spans that must never prompt: fenced code blocks, indented code blocks, inline backtick spans, and bare URLs",
    "files_affected": [
      "src/wordup/protect.py",
      "tests/test_protect.py"
    ],
    "acceptance_criteria": [
      "Returns a list of (start, end) offsets into the original string",
      "Fenced code blocks delimited by triple backticks are detected including the fence lines",
      "Indented code blocks of four spaces or a tab are detected",
      "Inline single-backtick spans are detected",
      "Bare http and https URLs are detected",
      "Overlapping spans are merged so the result is non-overlapping and sorted",
      "Detection is regex only, with no parser and no third-party dependency",
      "Tests cover each region type plus the overlap-merge case at 100% coverage of protect.py"
    ],
    "steps": [
      "Create src/wordup/protect.py with one compiled pattern per region type",
      "Implement protected_spans(text) returning merged, sorted, non-overlapping offsets",
      "Implement an overlaps(span, protected) helper for the scanner to consume",
      "Write tests/test_protect.py with a Markdown fixture containing all four region types",
      "Add a test asserting adjacent and nested regions merge correctly",
      "Run ruff format, ruff check --fix, mypy src/, and pytest -x",
      "Commit: feat(protect): detect code blocks and URLs as protected spans"
    ]
  },
  {
    "task": "scanner",
    "category": "core",
    "priority": 6,
    "done": false,
    "description": "R6 - Pure span-based scanner matching only the 86 base words, case-insensitively, word-boundary anchored, excluding protected spans",
    "files_affected": [
      "src/wordup/scanner.py",
      "tests/test_scanner.py"
    ],
    "acceptance_criteria": [
      "Matches are returned as (start, end) offsets into the original string",
      "Only base words trigger; none of the 526 alternatives ever match",
      "Matching is case-insensitive and word-boundary anchored",
      "Inflected surface forms match via the inflect module and carry the stripped suffix",
      "Matches overlapping a protected span are discarded",
      "Matches are returned in document order",
      "Alternatives are filtered through the R4 inflection guard before the Match is returned",
      "A match whose surface form required stemming carries no multi-word alternatives",
      "The scanner never calls print or input and never touches the filesystem",
      "Tests confirm directionality, case-insensitivity, protected exclusion, ordering, and multi-word filtering at 100% coverage"
    ],
    "steps": [
      "Read src/wordup/models.py, inflect.py, and protect.py",
      "Create src/wordup/scanner.py building one alternation pattern from the lexicon base words",
      "Implement scan(text, lexicon) using re.finditer over the raw text",
      "For each candidate token, try exact lexicon lookup then stemmed lookup",
      "Filter out matches overlapping any protected span",
      "Populate each Match with span, surface form, base word, stripped suffix, and offered alternatives",
      "Filter the alternative list through the inflect guard so multi-word entries drop out when a suffix was stripped",
      "Write tests/test_scanner.py asserting an alternative such as entry never matches",
      "Add a test asserting addresses offers no multi-word alternative while address does",
      "Run ruff format, ruff check --fix, mypy src/, and pytest -x",
      "Commit: feat(scanner): add directional span-based lexicon scanner"
    ]
  },
  {
    "task": "sentence-context",
    "category": "core",
    "priority": 7,
    "done": false,
    "description": "R7 - Extract the sentence containing a match for display, with an abbreviation guard and a decimal guard",
    "files_affected": [
      "src/wordup/context.py",
      "tests/test_context.py"
    ],
    "acceptance_criteria": [
      "Given a match offset, returns the containing sentence span",
      "Splits on terminal punctuation followed by whitespace and a capital letter",
      "Does not split at e.g., i.e., Dr., Mr., Mrs., vs., or etc.",
      "Does not split inside a decimal number such as 3.14",
      "A wrong boundary only clips the displayed window and never alters the match span",
      "Text with no terminal punctuation falls back to the containing block, meaning the run of text between blank lines, not the whole document",
      "Tests cover each abbreviation, the decimal guard, and the no-punctuation case at 100% coverage"
    ],
    "steps": [
      "Create src/wordup/context.py with the abbreviation exclusion list as module data",
      "Implement sentence_span(text, offset) returning the containing sentence offsets",
      "Add the abbreviation guard and the digit guard to the split pattern",
      "Add the fallback path returning the containing block between blank lines when no terminal punctuation is found",
      "Write tests/test_context.py covering every abbreviation and the decimal case",
      "Add a test asserting the match span is unchanged when the boundary is wrong",
      "Run ruff format, ruff check --fix, mypy src/, and pytest -x",
      "Commit: feat(context): add sentence window extraction with abbreviation guard"
    ]
  },
  {
    "task": "replacement-apply",
    "category": "core",
    "priority": 8,
    "done": false,
    "description": "R8 - Apply accepted choices by slicing the original string at match spans, transferring case and preserving all surrounding characters",
    "files_affected": [
      "src/wordup/apply.py",
      "tests/test_apply.py"
    ],
    "acceptance_criteria": [
      "Applying zero choices returns a string byte-identical to the input",
      "Newlines, blank lines, tabs, and runs of multiple spaces are preserved exactly",
      "split() and join() are not used anywhere in the module",
      "Access, becomes Entry, with the comma intact",
      "ACCESS. transfers to uppercase and keeps the period",
      "(access) keeps both parentheses",
      "access-control is handled without corrupting the hyphenated remainder",
      "Case transfer covers lowercase, Title case, and UPPERCASE",
      "Choices are applied right to left or via a single rebuild so earlier offsets stay valid",
      "Tests cover a multi-paragraph Markdown fixture and every punctuation and case case at 100% coverage"
    ],
    "steps": [
      "Create src/wordup/apply.py",
      "Implement transfer_case(surface, replacement) for lower, Title, and UPPER patterns",
      "Implement apply_choices(text, choices) rebuilding the string from spans in one pass",
      "Assert internally that spans are non-overlapping and sorted before applying",
      "Write tests/test_apply.py with a multi-paragraph fixture containing tabs and blank lines",
      "Add the byte-identical zero-choice test and every punctuation and case round-trip",
      "Run ruff format, ruff check --fix, mypy src/, and pytest -x",
      "Commit: feat(apply): add span-slicing replacement with case transfer"
    ]
  },
  {
    "task": "rendering",
    "category": "ux",
    "priority": 9,
    "done": false,
    "description": "R9 - Render the prompt as the containing sentence with the match marked, honoring NO_COLOR with a caret underline fallback",
    "files_affected": [
      "src/wordup/render.py",
      "tests/test_render.py"
    ],
    "acceptance_criteria": [
      "The matched word is marked with ANSI bold or reverse video by default",
      "A caret underline on the following line marks the match position",
      "When NO_COLOR is set to any value, no \\x1b byte appears in the output",
      "The caret underline is present in both the color and no-color paths",
      "The caret aligns with the match even when the sentence window is clipped",
      "Option 0 is rendered as NO CHANGE",
      "Options 1..n show alternatives in their exact insertable form including inflection",
      "The pre-selected default option, when present, is visibly marked",
      "render is pure: it returns a string and does not write to a stream",
      "Tests set and unset NO_COLOR and assert on escape-byte presence at 100% coverage"
    ],
    "steps": [
      "Create src/wordup/render.py",
      "Implement color_enabled() reading the NO_COLOR environment variable",
      "Implement render_prompt(sentence, match, alternatives, default) returning a string",
      "Compute caret offsets relative to the rendered sentence window",
      "Write tests/test_render.py using monkeypatch to set and clear NO_COLOR",
      "Assert no \\x1b byte appears when NO_COLOR is set and the caret line is still present",
      "Run ruff format, ruff check --fix, mypy src/, and pytest -x",
      "Commit: feat(render): add NO_COLOR-aware prompt rendering"
    ]
  },
  {
    "task": "interactive-session",
    "category": "ux",
    "priority": 10,
    "done": false,
    "description": "R10 - Prompt loop over matches in document order with remembered alternative defaults, unremembered refusals, and no suppression keys",
    "files_affected": [
      "src/wordup/interactive.py",
      "tests/test_interactive.py"
    ],
    "acceptance_criteria": [
      "Every occurrence of every match is prompted; no suppression mechanism exists",
      "A previously chosen alternative is pre-selected as the default at later occurrences of that base word",
      "Pressing Enter accepts the pre-selected default",
      "Choosing NO CHANGE writes nothing to remembered state: it neither creates a default nor clears an existing one",
      "Choose an alternative, then choose NO CHANGE, then meet the word a third time: the original alternative is still the default (see Open question 7)",
      "A word refused at its first occurrence shows no default at its second",
      "Accepted inputs are 0 through n and Enter only",
      "Invalid input reprompts the same match rather than being treated as no change (see Open question 3)",
      "KeyboardInterrupt applies decisions so far, emits the remainder untouched, and exits 130 (see Open question 2)",
      "The module reads input through a single injectable function so tests need no real terminal",
      "Prompts are written to stderr, never stdout",
      "Tests drive a full session with a scripted input sequence at 100% coverage"
    ],
    "steps": [
      "Read src/wordup/render.py, context.py, and scanner.py",
      "Create src/wordup/interactive.py with an injectable read function defaulting to input",
      "Implement the loop over matches in document order, rendering each via render_prompt",
      "Track remembered choices in a dict keyed by base word, storing alternatives only",
      "Skip storing a remembered value when the user answers 0, leaving any existing entry untouched",
      "Add the invalid-input reprompt path and the KeyboardInterrupt handler",
      "Write tests/test_interactive.py scripting inputs and asserting remembered-default behavior",
      "Add a test asserting a refusal at first occurrence produces no default at the second",
      "Add a test for the choose, refuse, then meet again sequence asserting the original choice survives",
      "Run ruff format, ruff check --fix, mypy src/, and pytest -x",
      "Commit: feat(interactive): add prompt session with remembered alternative defaults"
    ]
  },
  {
    "task": "public-api",
    "category": "core",
    "priority": 11,
    "done": false,
    "description": "R11 - Export a small pure library surface from __init__.py with no terminal or filesystem I/O and deliberately no automatic improve() helper",
    "files_affected": [
      "src/wordup/__init__.py",
      "tests/test_public_api.py"
    ],
    "acceptance_criteria": [
      "suggest(text, lexicon=None) -> list[Match] is exported",
      "apply_choices(text, choices) -> str is exported",
      "Lexicon.default() and Lexicon.load(path) are exported",
      "No improve(text) -> str function exists, since automatic rewriting is a non-goal",
      "No exported callable selects choices on the caller's behalf; the consumer supplies choices from their own interface",
      "__all__ lists exactly the public surface",
      "Importing wordup does not read the filesystem or load the lexicon at import time",
      "Both functions run with stdin closed and produce no output on any stream",
      "Tests exercise the full surface without a terminal at 100% coverage"
    ],
    "steps": [
      "Read src/wordup/scanner.py and apply.py for the underlying callables",
      "Populate src/wordup/__init__.py with the four exports and an explicit __all__",
      "Make lexicon loading lazy so import time stays free of file I/O, per RULES §14",
      "Write tests/test_public_api.py running both functions with stdin closed",
      "Add a test asserting importing wordup performs no file read",
      "Add a test asserting no improve attribute is exported",
      "Run ruff format, ruff check --fix, mypy src/, and pytest -x",
      "Commit: feat(api): expose pure suggest and apply_choices library surface"
    ]
  },
  {
    "task": "cli",
    "category": "cli",
    "priority": 12,
    "done": false,
    "description": "R12 - argparse entry point with positional text, -f, -o, --in-place, stderr-only prompts, and a hard no-terminal failure",
    "files_affected": [
      "src/wordup/cli.py",
      "tests/test_cli.py"
    ],
    "acceptance_criteria": [
      "A quoted positional argument is reviewed directly",
      "-f PATH reads the document from a file, one file per invocation",
      "-o PATH writes the result to a file",
      "--in-place overwrites the input file and is valid only with -f",
      "Default output is stdout",
      "All prompts, menus, and status messages go to stderr",
      "wordup -f draft.md > out.md produces a file containing no menu text",
      "The positional argument and -f are mutually exclusive",
      "Supplying neither the positional argument nor -f is an error",
      "sys.stdin.isatty() is checked before any input is read",
      "With no controlling terminal the process prints an error naming the cause, writes nothing, and exits 2",
      "main(argv) returns an integer exit code and is callable from tests",
      "Tests cover every flag combination and the no-tty path at 100% coverage"
    ],
    "steps": [
      "Read AGENTS/skills/cli-development.md for the argparse patterns used in this repo",
      "Create src/wordup/cli.py with build_parser() and main(argv) returning an exit code",
      "Add the mutually exclusive group for the positional argument and -f",
      "Add the tty check before any file read, returning exit code 2 on failure",
      "Add the --in-place guard rejecting use without -f before opening anything",
      "Wire stdout for the result and stderr for all prompts and messages",
      "Write tests/test_cli.py driving main(argv) with capsys and a monkeypatched isatty",
      "Run ruff format, ruff check --fix, mypy src/, and pytest -x",
      "Commit: feat(cli): add argparse entry point with file and text input modes"
    ]
  },
  {
    "task": "error-handling",
    "category": "core",
    "priority": 13,
    "done": false,
    "description": "R13 - Explicit typed exception handling across every input and data path, with no bare except anywhere, per RULES §9",
    "files_affected": [
      "src/wordup/errors.py",
      "src/wordup/cli.py",
      "tests/test_errors.py"
    ],
    "acceptance_criteria": [
      "No bare except appears anywhere in src/",
      "FileNotFoundError on -f produces a message naming the path",
      "PermissionError on -f or -o produces a message naming the path",
      "IsADirectoryError on -f produces a message naming the path",
      "UnicodeDecodeError on -f produces a message naming the path and the encoding problem",
      "--in-place without -f produces a specific message and is rejected before any read",
      "-o pointing at an unwritable path produces a message naming the path",
      "Malformed lexicon TOML produces a typed error, never a raw tomllib traceback",
      "Each lexicon validation failure from R3 surfaces with the offending word in the message",
      "No traceback reaches the user for any handled condition",
      "Tests trigger every listed condition at 100% coverage"
    ],
    "steps": [
      "Read src/wordup/errors.py and cli.py",
      "Extend the exception hierarchy to cover every condition listed in R13",
      "Add explicit handlers in cli.py mapping each exception to a message and an exit code",
      "Grep src/ for bare except and for except Exception and remove any occurrence",
      "Write tests/test_errors.py triggering each condition with tmp_path fixtures",
      "Assert no traceback text appears on stderr for handled conditions",
      "Run ruff format, ruff check --fix, mypy src/, and pytest -x",
      "Commit: fix(errors): add explicit typed handling for all input and data failures"
    ]
  },
  {
    "task": "readme",
    "category": "docs",
    "priority": 14,
    "done": false,
    "description": "R14 - Write README.md covering installation, both invocation forms, every flag, prompt behavior, and the three documented limits, per RULES §4",
    "files_affected": [
      "README.md"
    ],
    "acceptance_criteria": [
      "Documents installation via uvx wordup \"text\" and via local install",
      "Documents the quoted positional argument form and the -f form",
      "Documents -o, --in-place, and the stdout default",
      "Documents that prompts go to stderr so output can be redirected cleanly",
      "Explains the numbered menu and option 0 for NO CHANGE",
      "Explains that a previously chosen alternative becomes the default at later occurrences",
      "Explains that NO CHANGE is never remembered and reprompts every time",
      "Documents NO_COLOR behavior and the caret underline fallback",
      "Documents the no-terminal error and its exit code",
      "States the three limits: no irregular verbs, no multi-word inflection, no user lexicon overrides",
      "Contains no em dashes and no emojis, per the repo writing style"
    ],
    "steps": [
      "Read src/wordup/cli.py for the final flag surface",
      "Write README.md with sections for install, usage, flags, the prompt, and limits",
      "Include worked examples for both invocation forms and for output redirection",
      "Grep the file for em dash characters and emoji and remove any found",
      "Commit: docs(readme): document installation, usage, flags, and known limits"
    ]
  },
  {
    "task": "retire-legacy-implementation",
    "category": "cleanup",
    "priority": 15,
    "done": false,
    "description": "R15 - Confirm every trace of the legacy script is gone from src/wordup/__init__.py, which now holds it pending replacement by the public API surface",
    "files_affected": [
      "src/wordup/__init__.py"
    ],
    "acceptance_criteria": [
      "No GrammarReviewer class remains in src/wordup/__init__.py",
      "No inline replacements dict remains; the lexicon lives only in src/wordup/data/lexicon.toml",
      "No review_text, display_suggestions, or add_replacement method remains",
      "No if __name__ == \"__main__\" block and no hardcoded path/to/your/file.txt remains",
      "No file in src/, tests/, or README.md references GrammarReviewer or grammar_reviewer",
      "The full test suite passes after removal",
      "The lexicon parity check from lexicon-data-migration was re-run before the legacy dict was removed"
    ],
    "steps": [
      "Re-run the lexicon parity check to confirm the TOML matches the legacy dict exactly",
      "Grep the repository for grammar_reviewer, GrammarReviewer, review_text, display_suggestions, and add_replacement",
      "Remove every remaining legacy symbol from src/wordup/__init__.py, leaving only the public API surface",
      "Run the full test suite to confirm nothing depended on the legacy code",
      "Commit: chore: remove legacy GrammarReviewer implementation"
    ]
  },
  {
    "task": "coverage-gate",
    "category": "testing",
    "priority": 16,
    "done": false,
    "description": "R16 - Close remaining coverage gaps, add the cross-module integration tests from the grill session, and commit a fail-under coverage threshold",
    "files_affected": [
      "pyproject.toml",
      "tests/test_integration.py"
    ],
    "acceptance_criteria": [
      "pytest reports 100% coverage across src/wordup",
      "Coverage configuration with fail-under is committed in pyproject.toml",
      "An integration test runs a full session over a multi-paragraph Markdown fixture end to end",
      "An integration test confirms a base word inside a fenced block, inline code, and a URL is never offered while the same word in prose is",
      "An integration test confirms no output ever contains deal withs or apply fored",
      "An integration test confirms a zero-choice session returns byte-identical text",
      "No CI workflow is added; that is deferred pending Open question 8"
    ],
    "steps": [
      "Read AGENTS/skills/python-testing.md for the repo pytest conventions",
      "Run pytest with coverage and identify every uncovered line",
      "Write tests/test_integration.py with the end-to-end Markdown fixture session",
      "Add the protected-region, multi-word, and byte-identical integration assertions",
      "Add coverage configuration with fail-under to pyproject.toml",
      "Run the full suite locally and confirm 100% coverage",
      "Commit: test: add integration coverage and fail-under threshold"
    ]
  }
]
